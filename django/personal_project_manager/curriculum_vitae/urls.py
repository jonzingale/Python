from django.conf.urls import url
from . import views

urlpatterns = [
    # ex: /books/
    # url(r'^$', views.index, name='index'),
    # ex: /books/5/
    # url(r'^(?P<project_id>[0-9]+)/$', views.detail, name='detail'),
    # ex: /books/books/new/
    # url(r'^projects/new/$', views.projects_new, name='projects_new'),
]